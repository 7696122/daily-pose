# Run Tests

Run the test suite for the Daily Pose project.

```bash
# Run all tests
npm test

# Run tests in watch mode
npm test -- --watch

# Run tests with coverage
npm test -- --coverage
```

If tests don't exist yet, offer to set up Vitest with React Testing Library.
